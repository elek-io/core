---
'@elek-io/core': minor
---

Added `FieldDefinitionGroup` schema and type to `fieldSchema.ts`, allowing FieldDefinitions inside a Collection to be organised into named, described groups for display as fieldsets in the UI
